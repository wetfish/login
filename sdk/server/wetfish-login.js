module.exports = {
    // Some functions for verifying tokens?
    // Requires curl
    // Intended to be loaded server-side
    // Maybe some day there will be SDKs for other languages too ^_~
    
    // An init function which requires a user ID and app ID
};
